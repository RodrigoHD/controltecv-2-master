<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ittepic.controltec">

    <!-- Para la libreria Picaso que carga las fotos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Solicitud de practicas"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="Solicitud de practicas"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity
            android:name=".Main3Activity"
            android:label="@string/title_activity_main3"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PreferenciasActivity"
            android:label="@string/title_activity_preferencias" />
        <activity
            android:name=".RegistrodePracticaActivity"
            android:label="@string/title_activity_Registro"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ActivityDetallesPractica"
            android:label="@string/title_activity_detalles_practica"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>